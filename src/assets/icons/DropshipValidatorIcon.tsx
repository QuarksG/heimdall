// src/assets/icons/DropshipValidatorIcon.tsx
// Re-exports the shared filled "invoice with check" glyph so the sidebar
// map stays stable. If we ever want to differentiate this from the
// reconciliation icon, swap the import here.
export { default } from "./InvoiceCheckIcon";
